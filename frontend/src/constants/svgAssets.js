// Mapeia as chaves que vêm do backend (ex: `fase.imagem`) para os SVGs locais do app.
// Adicione aos poucos conforme for criando os demais arquivos em `frontend/assets/`.

export const svgAssetsPorChave = {
  // Exemplos (ajuste depois se você criar SVGs reais com estes nomes).
  bracos: require('../../assets/abraco-vovo.svg'),
  cabeca: require('../../assets/presente.svg'),
};

